# 项目协作规则

## 本地开发服务器

- Codex 可以自行启动本地开发服务器，不需要用户手动操作。
- 启动前先检查现有服务，并优先复用固定端口 `3000`。
- 禁止因为端口占用而自动切换到 `3001`、`3002` 等新端口。
- 如果没有可用服务，Codex 可以自动启动或重启；不限制任务期间的累计启动次数。
- 同一项目同时只允许存在一个开发服务器实例。每次启动前必须检查固定端口和此前记录的 PID，清理已失效或属于本项目的旧实例后，再在同一端口启动。
- 每次启动必须使用固定端口、独立且唯一的日志文件，并记录 PID；不得通过自动换端口规避冲突。
- 启动后使用带超时的健康检查确认服务可用；确认成功后，启动命令必须立即返回，不得持续占用 Codex 命令执行状态。
- 若固定端口已被其他进程占用，先识别该进程；属于本项目则复用，不属于本项目则停止自动启动并说明冲突。
- 浏览器验证失败时可以重启本项目服务后再验证；连续两次仍失败则停止服务重试，改用 `lint`、`typecheck`、`test` 和 `build` 验证并说明原因。
- 不得因视觉复核阻塞已经通过自动检查的任务交付。
- 临时服务验证结束后应关闭此前记录的 PID；不得批量终止所有 `node` 进程。
