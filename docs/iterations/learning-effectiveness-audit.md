# 学习成效口径审计

日期：2026-08-09
审计基线：`54ab960`
本轮目标：第 39 轮只读审计未来处置维度的 sessionId 证据边界；不改变第 38 轮 B1/B2/B3，不实现分维度报告。

## 指标矩阵

| 指标 | 数据源 | 分子 | 分母 | 时间窗 | 配对方式 | 能证明 | 不能证明 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 冲刺次数 | `reviews[].sessionId`；`buildSprintHistory` / `buildSprintEffectiveness` | 不适用；计数值为不同 `sprint:*` session 数 | 有至少一条可识别冲刺 review 的 session | 历史卡片为全部历史；周报为本地本周 | 不涉及配对；按 sessionId 去重 | 冲刺活动发生次数 | 训练质量、保持效果或因果收益 | ✅ 口径诚实 |
| 覆盖词数 | 冲刺 reviews 的 `wordId` | 不适用；计数值为不同 wordId 数 | 有 wordId 的冲刺 review | 历史卡片为全部历史；周报为本地本周 | 同一统计窗内按 wordId 去重 | 冲刺触达的不同词数 | 每词训练强度、是否掌握或后续保持 | ✅ 口径诚实 |
| 近 7 天评分达标事件占比 | `buildLearningInsights` 消费的全部合法时间 review；不区分 new/review/sprint | `rating≥2` 的评分事件数 | 窗内全部评分事件数；当前窗无事件时 `null` | 当前窗：本地当天向前共 7 个自然日的 00:00 至 `now`（含）；上一窗：再向前 7 个自然日的 00:00（含）至当前窗起点（不含） | 不按词去重；每个评分事件一票，同词和混合会话可重复进入；未来与无效时间排除 | 两个独立窗口各自记录到的评分事件达标占比，以及两窗均有样本时的百分点差 | 同词变化、样本构成一致、学习效果提升、冲刺收益或后续保持 | ✅ 口径诚实 |
| 冲刺事件平均回忆耗时 | 冲刺 reviews 的合法 `recallMs`；仅冲刺历史卡保留此活动描述 | 合法 `recallMs` 之和 | 合法测时 review 条数 | 单次历史冲刺 session | 不按词配对；review 事件加权，同词可多次进入 | 被记录冲刺评分事件的平均反应耗时 | 每词等权表现、前后变化、后续保持或训练导致的变化 | ✅ 口径诚实 |
| 同词配对回忆变化 | `buildPairedRecallChange`：目标冲刺事件的合法 `recallMs`，以及每个目标词在边界前最近一条非冲刺合法 `recallMs` | 每个配对词先算目标事件词内均值，再跨词等权；变化为目标均值减基线均值 | 两侧都有合法样本的去重 wordId；无配对时 count=0、均值与变化为 `null` | 周报：本地周窗内全部冲刺事件，边界为该窗首个冲刺事件；完成页：完整 session，边界为 `session.createdAt` | 严格按 wordId 配对；基线只取最近非冲刺一条，目标侧同词多事件先聚合 | 已记录配对词在对应冲刺事件集内，相对其最近非冲刺记录的平均回忆变化 | 后续正常复习保持、未测时词表现、训练导致的提升或维度因果 | ✅ 口径诚实 |
| 当场达标词数（内部兼容字段 `resolvedCount` / `successCount`） | 冲刺 reviews；`rating≥2` 的 wordId | 不适用；计数值为至少一次 `rating≥2` 的去重词数 | 对应周或单次冲刺覆盖的去重词集 | 周报为本地本周；完成页与历史卡为单次冲刺 session | 按 wordId 去重，但不要求最终一次达标，也无后续随访 | 冲刺中至少一次当场评分达标的词数 | 已解决、已恢复、长期掌握或后续保持 | ✅ 口径诚实 |
| 4 周冲刺观察 | `buildSprintEffectivenessSeries`，逐周复用冲刺次数、当场达标词与同词配对规则 | 各周独立计算，不跨周合并配对 | 每周各自的配对词数；无冲刺周为 `null`，有冲刺但无配对显示无配对样本 | 含本周的最近 4 个本地周；每周边界为该周首个冲刺事件 | 每周内按 wordId 配对、跨词等权 | 最近四周各自的冲刺活动量、当场表现与配对回忆观察 | 后续保持、跨周同 cohort 演变或因果提升 | ✅ 口径诚实 |
| 4 周复发率（第 34 轮更名为“冲刺后当前仍薄弱率”） | 最近 4 个完整周 `rating≥2` 冲刺 wordId；当前 `buildWordWeakSignals` | 最近 4 周窗口内最近一次达标处置周 cohort 中当前画像非空的去重词数 | 该周 cohort 去重词数；同词跨周只归窗口内最近一次达标处置周；空 cohort 为无样本 | 处置窗为当前周前 4 个完整本地周；结果时点为当前 | 以 wordId 将处置 cohort 与当前画像配对 | 某次冲刺当场达标后，截至当前仍被统一画像判弱的比例 | 历史周末状态；从未恢复还是恢复后再次薄弱；严格复发时间；训练导致的结果 | ✅ 口径诚实 |
| 冲刺后首次正常复习保持 4 周 | `buildSprintRetentionSeries` 消费 `reviews[].id/wordId/reviewedAt/rating/sessionId/recallMs` | 保持：已观察且首条非 sprint review `rating≥2` 的词数；覆盖：已有首条非 sprint review 的词数 | 保持分母：已观察词；覆盖分母：成功冲刺 cohort；未观察和截断不进保持失败分母 | 当前本地周一之前最近 4 个完整周；每词只归窗口内总序最近成功冲刺锚点所在周；随访扫描至 `now` | `(reviewedAtMs,id)` 总序；锚点后首条 review 若为 sprint 则截断，否则为随访；`quiz:*` review 可随访，`quizAttempts` 不参与 | 已记录成功冲刺词在下一次冲刺前是否已有首次正常评分、该次评分是否达标、实际间隔及合法同词测时变化 | 未观察词的真实保持结果、评分间其他学习行为、冲刺导致保持/恢复/掌握或维度因果 | ✅ 口径诚实 |

## 第 34 轮修正证据

修改前，同词只要在多个周各有一次冲刺 `rating≥2`，就会同时进入多个周 cohort，4 周展示跨周重复样本；页面又把当前统一薄弱画像写成“复发”，暗示已经恢复后发生了新的历史事件。

修改后：

- 多周序列按 wordId 只归最近一次达标处置周，输入 review 顺序不影响归属。
- UI 统一使用“当场达标”“当前仍薄弱”“当前仍薄弱率”。
- UI 明示“未区分从未恢复与恢复后再次薄弱”。
- 既有单周派生接口、当前仍薄弱词明细和再次冲刺动作保留；不改评分、FSRS、恢复判定或数据写入。

## 第 35 轮修正证据

- `buildSprintEffectiveness.resolvedCount` 仍严格来自本周 `sprint:*` review 中 `rating≥2` 的 wordId 去重；`buildSprintCompletionSummary.resolvedCount` 与冲刺历史 `successCount` 是单次 session 的相同口径。
- 周报标题、指标、4 周说明、每周卡片、冲刺完成页和冲刺历史成功率共 6 个用户入口统一改为“当场达标”；内部字段、评分条件、去重和数值断言均保留。
- “仍需关注”继续来自当前统一薄弱画像，允许与当场达标词重叠；未把它改写成“未达标”。

## 第 36 轮修正证据

- 新增共享纯函数 `buildPairedRecallChange`：目标侧按 wordId 聚合全部合法测时后取词内均值；基线侧只选严格早于边界的最近一条非冲刺合法测时；两侧都有样本才进入配对词分母，最后跨词等权。
- 周报与 4 周趋势的目标事件是对应本地周窗内全部 `sprint:*` review，边界是该窗第一个冲刺事件；完成页目标事件是完整 session 的 review，边界是 `session.createdAt`。两入口共享同一配对规则但保留各自真实目标窗。
- 完成页基线明确排除历史 `sprint:*`；不改写任何历史 review。无配对返回 `pairedWordCount=0` 且两个均值与变化均为 `null`，三个入口均显示“无配对样本”。
- 用户文案统一为“冲刺观察 / 同词配对回忆变化 / 较此前快或慢”，明确配对词数；删除“提升/降幅/冲刺后”因果或时序暗示。
- 纯函数测试覆盖同词两侧、目标词内多事件、跨词等权、最近非冲刺基线、历史冲刺排除、无配对、缺失/非法测时、严格边界和乱序输入；周报、4 周与完成页均有集成/E2E 证据。
- 历史 `recallMs` 覆盖不足只会减少配对样本并诚实显示无样本；没有补写、回填或伪造测时。

## 第 37 轮修正证据

- `LearningInsights.successRate` 从 `number` 改为 `number | null`；当前窗无评分事件时返回 `null`，有合法事件但全部 `rating<2` 时仍返回真实 `0`。`successRateDelta` 仅在当前窗和上一窗都有事件时计算，单位明确为百分点。
- 统计规则不变：当前窗为本地当天向前共 7 个自然日的 00:00 至 `now`，上一窗为紧邻的前 7 个自然日且右边界不含当前窗起点；未来和无效时间排除。
- 分子仍为 `rating≥2` 事件数，分母仍为窗内全部评分事件数；不按词去重，每事件一票，同词重复以及 new/review/sprint、普通/冲刺 session 混合均保留。
- 轨迹页改为“评分达标占比”，披露“`rating≥2 / 全部评分事件`”和“近 7 天截至目前”。当前无样本显示“— / 当前窗无样本”，真实 0 显示 `0%`；两窗都有样本时显示“较上窗 ±N 个百分点”或“较上窗持平”，上一窗无样本显示“无可比上窗”。
- 该比较只是两个事件总体占比之差，不做同词配对，不假设两窗构成一致，也不写成提升、下降原因或学习成效。
- 单测覆盖当前无样本且上一窗有样本、真实 0 且上一窗有/无样本、真实 100%、同词多事件、混合会话、本地窗口左右边界、未来和无效时间；E2E 覆盖无样本、真实 0、百分点差与持平用户入口。

## 第 38 轮 B1/B2/B3 证据

- 只读审计确认现有 `ReviewEvent` 已有稳定的事件 id、词 id、发生时间、评分、可选 sessionId 与可选 recallMs；普通 review 可由“非 `sprint:*`”识别。`quizAttempts` 是独立数组，不会进入仅消费 reviews 的观察函数，因此无需 schema/version/store/domain。
- B1：最近 4 个完整周内，每个 wordId 只选 `(reviewedAtMs,id)` 总序上最近一次 `rating≥2` 的 sprint review 为锚点，并归入该锚点所在周。锚点后同词第一条 review 若仍为 sprint（任意评分、任意 sprint session）即截断；否则该非 sprint review 是首次随访。`quiz:*` review 和无 sessionId 旧 review 均可随访，不能排除。
- B2：cohort、已观察、未观察、截断与保持词数分别披露；覆盖率分母是 cohort，保持率分母仅是已观察词。未观察（含截断）不充当失败。cohort 空周返回 `null`；已观察为 0 时保持率与平均间隔为 `null`，真实 0% 保持率保留。
- B3：只有锚点与已配对随访两侧 `recallMs` 都是有限且 `>=0` 的词进入测时分母；两侧均值先词级配对后跨词等权，变化为“随访 − 冲刺”。评分保持分母与测时分母独立，缺失/非法测时不撤销已观察事实。
- 页面在当前仍薄弱区后展示“冲刺后首次正常复习保持 4 周”，逐周披露 cohort、覆盖、保持、未观察、截断、实际平均间隔、配对测时数和两侧均值；文案持续标注观察性，不声称导致、恢复或掌握。
- 单测覆盖成功/失败随访、无后续、下一 sprint 成功/失败截断、跨周最近锚点、同 session 多事件、quiz review 答对/答错、quiz review 与下一 sprint 先后、只有 quizAttempt、同毫秒总序、乱序、非法/未来时间、空 cohort、零观察与合法/非法测时。

## 阶段 A/B 结论与阶段 C 入口

1. 阶段 A 完成：冲刺活动量、当场达标、同词配对回忆观察、截至当前仍薄弱，以及近 7 天全局评分事件占比，现已在分子、分母、时间窗、权重、空样本与不能证明的结论上明确分开。
2. 阶段 B 的 B1 cohort/截断、B2 覆盖/保持与 B3 配对测时已在同一权威纯函数和同一用户入口闭环；没有跨新冲刺、没有把未观察算失败，也没有把 quizAttempt 当 review。
3. 猜错仍只有累计次数，没有真实事件时间、恢复或复发数据源；禁止伪造，但不阻止阶段 A/B 的诚实口径完成。
4. 下一轮只进入阶段 C“未来处置维度归因”的只读审计：严格复用现有结构化 `sprint:*` sessionId，先证明能否无歧义连接处置维度与未来首次正常 review；不新增 schema、不回填历史、不先实现分维度报告或自适应。

## 第 39 轮未来维度归因边界

| 指标/候选事实 | 数据源 | 分子 | 分母 | 时间窗 | 配对方式 | 能证明 | 不能证明 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 旧冲刺处置维度 | 旧 `reviews[].sessionId=sprint:<ISO>` | 不适用 | 所有旧普通 sprint session | 全历史 | 只能按完整 sessionId 聚合同场 review | 是冲刺 review、事件时间、评分、词与合法测时 | 拼写、中译英、辨析、查词、慢回忆、lapse 或通用处置中的任一种 | ⚠️ 历史不可归因 |
| 顽固结构化处置维度 | `sprint:stubborn:<mode>:<ISO>` | 不适用 | 合法模式与合法时间的顽固 session | 格式启用后的历史 | sessionId 精确归属；主维度为 stubborn，mode 只作子模式 | 顽固处置及主动回忆/听音拼写/中译英子阶段 | 普通拼写/中译英薄弱专项，或该模式导致保持 | ✅ 口径诚实 |
| 未来统一处置维度 | 拟由 `sprint:treatment:<dimension>:<ISO>` 承载；当前尚未实现 | 以后只能统计解析成功且维度已知的新 session | 必须按每个指标分别披露已知维度样本；unknown 单列，不进入已知维度分母 | 仅新编码启用后的未来数据 | 维度绑定 sprint 锚点；B 链仍按 wordId、总序与下一 sprint 截断 | 入口启动时明确记录的处置维度，以及其后真实首次正常 review | 历史未知维度、模式因果、未随访结果、跨新 sprint 的结果 | ➖ 当前数据不可证明 |

审计确认 `ReviewEvent.sessionId`、activeSession/activeQuiz 和现有分域足以承载未来字符串，不需要 schema/version。当前仍缺统一编码器、解析器与 startedAt 提取；`buildSprintHistory` 会把拟议的新格式后缀整体当日期并过滤，因此阶段 C 尚未完成。

## 第 40 轮未来维度记录边界

- 新 treatment、旧普通和顽固 session 都继续以 `startsWith("sprint:")` 进入现有活动量、当场达标、同词配对、当前仍薄弱 cohort 与首次正常复习保持；本轮没有按维度重算或改变任何指标。
- 新维度只是锚点 session 的启动事实，不改变 word cohort、评分阈值、时间窗、词级去重、随访覆盖、保持分母、未观察、实际间隔或配对测时分母。
- 任一下一 sprint 即截断的规则保持；不同 treatment 维度同样截断。`quiz:*` review 和无 sessionId review 仍可作为普通随访，quizAttempt 仍不进入保持链。
- 旧普通 sprint 永远是 `unknown`；未来明确混合处置是 `generic-sprint`。后续阶段 D 必须把二者分开并显示样本数，不能把旧历史分摊到已知维度。
- slow-recall/lapse 暂无不复制画像规则的可靠启动级唯一判定；本轮仅保留枚举能力，所有 fallback 写 generic，不形成虚假可归因样本。
- 单测与全量测试证明新格式不改变 A/B 数字；固定 3000 的 signal-flow 17/17 进一步证明真实入口、刷新、写入、历史与 generic 复跑闭环，阶段 C 完成。首次 16/17 失败来自旧 lookup 信号被成功 review 正确降级，修正版用真实再次划词触发复发，不改变任何指标或门槛。

下一轮的证据纪律：

- 旧 `sprint:<ISO>` 在数据层一律是 `unknown`；不得因用户文案写“未标注/通用冲刺”就把它并入未来 `generic-sprint`。
- 三类 Quiz 与 lookup 的现有结构化推荐可在启动时直接编码；顽固保留现有格式并解析为主维度 stubborn + 子 mode。
- slow-recall/lapse 只有当启动当下的完整词集由结构化事实证明为单一维度时才可编码；多维、混合、猜错或不确定词集写 `generic-sprint`，禁止解析中文标签。
- B 链不因维度编码改变：最近成功锚点、任意下一 sprint 截断、`quiz:*` review/无 sessionId review 可作随访、quizAttempt 不参与、未观察不进失败分母，全部保持。
- 在统一未来编码纵向链完成前，不产出偏样本分维度保持率、排行或自适应结论。

## 第 41 轮：分维度观察报告指标矩阵

日期：2026-08-09 ｜ 基线：`cf46fdc` ｜ 唯一函数：`buildDimensionObservationReport`（`lib/weak-signals.ts`）

| 指标 | 数据源 | 分子 | 分母 | 去重键 | 维度归属 | 合计安全性 | 空/null/真实 0 |
|---|---|---|---|---|---|---|---|
| 冲刺 session 数 | 窗口内 `sprint:*` review 的 `sessionId` | 计数值 | 该维不同 sessionId | sessionId | `parseSprintSessionId` | ✅ 分维合计==全局 | 0 会话显示无样本 |
| 覆盖词数（活动） | 窗口内冲刺 review 的 `wordId` | 计数值 | 该维去重 wordId | wordId | sessionId→parser | ⚠️ 同词跨维可重复，不可合计 | 0 显示无样本 |
| 当场达标词数（活动） | 窗口内冲刺 review `rating≥2` | 计数值 | 该维去重 wordId | wordId | sessionId→parser | ⚠️ 同词跨维可重复，不可合计 | 0 显示无样本 |
| 成功锚点 cohort | 全局总序最近 `rating≥2` 冲刺（同 `buildSprintRetentionSeries`） | 计数值 | 该维唯一锚点词数 | wordId（唯一锚点） | 锚点 sessionId→parser | ✅ 分维合计==全局 | cohort=0 → 保持系全 null |
| 随访覆盖数/率 | 锚点后首条 review 非 sprint（含 quiz:*/无 session） | `followedUpCount` | cohort | wordId | 继承锚点 | ✅ 计数可合计 | cohort>0 无随访 → 覆盖 0%（真实 0） |
| 保持成功数/率 | 随访 review `rating≥2` | `retainedCount` | `followedUpCount` | wordId | 继承锚点 | ✅ 计数可合计 | 无已观察 → 保持率 null |
| 实际随访间隔 | 已观察词锚点→随访间隔 | 间隔和 | `followedUpCount`（词等权） | wordId | 继承锚点 | 计数可合计；均值不合计 | 无已观察 → null |
| 配对测时样本/变化 | 锚点与随访两侧合法 `recallMs` | 配对词数 | 该维双侧合法样本对数 | wordId | 继承锚点 | ✅ 样本数合计==全局 | 无配对 → sampleCount=0、均值/变化 null；合法 0 保留 |
| 当前仍薄弱数/率 | cohort ∩ 当前 `buildWordWeakSignals` 非空 | 该维仍薄弱词数 | 该维全部唯一成功锚点 cohort（含未观察与截断） | wordId | 继承锚点 | ✅ 计数合计==全局 | cohort=0 → rate null；全弱/全不弱 → 100%/0% |

关键口径结论：

- 仍薄弱分母为全部唯一成功锚点 cohort，未观察/截断词仍参与判定；该指标与 B 链随访可观测性完全解耦，等价于第 34 轮 `buildSprintCohortRelapse` 按维度分区。
- 锚点系计数分维合计恒等于全局同窗值（每词/每 session 唯一归属）；活动系覆盖/当场达标允许跨维重复，UI 与测试均显式标注不可跨维合计。
- 四种分母独立（当场达标/仍薄弱/保持/配对测时），无样本显示“无样本/—”，真实 0 保留；不合并成综合分数，不排行。
- 维度只由 `parseSprintSessionId` 得出；旧普通/未知 treatment/非法顽固=unknown，generic-sprint 与 unknown 分列，顽固子 mode 只披露不扁平，不从标签/标题/画像/attempt 反推。
- 分维观察不改变任何既有指标：全量 211/211、signal-flow 18/18，阶段 A/B/C 数字保持不变（见 `round-41.md`）。
