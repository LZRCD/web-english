# 第 17~26 轮自主迭代循环最终报告

> 收尾日期：2026-08-08 ｜ 分支：`codex/follow-up-hardening` ｜ 功能起点：`4cc0852`

## 基线与决策

- 第 0 轮只读复查以 `b45fd84` 为基线：测试主体 155/155 通过，核心咬合为 5 ✅ / 3 ❌ / 0 ⚠️；确认先补入口口径、时间线和稳定提示断链，不先扩展孤立功能。
- 第 20 轮后方向审视：核心咬合推进至 7 ✅ / 1 ❌，决定第 21 轮优先补齐普通复习时间线，并禁止为“猜错”伪造事件时间。
- 第 25 轮后方向审视：核心 8 项已全部 ✅，lapse、慢回忆、测验恢复及统一反馈均咬合；仅选择仍有中高价值的 4 周冲刺处置 cohort 复发率作为第 26 轮目标。
- 第 26 轮整体评估：14 项联动检查均为 ✅，除 schema 边界阻塞的“猜错”外无剩余中高价值断链，决定不续跑第 27~31 轮。

## 第 19~26 轮交付

| 轮次 | 目标 | 提交 |
|---|---|---|
| 19 | 统一词书、划词候选、冲刺候选的实时薄弱画像口径 | `c7124ad` `feat: 统一薄弱画像各入口口径` |
| 20 | 收紧查词“已稳定”提示的当前阈值与无其他弱点门禁 | `1e42c45` `fix: 收紧查词已稳定提示门禁` |
| 21 | 让普通四档复习与冲刺复习可靠进入词级时间线 | `f34ebd7` `fix: 补齐普通复习时间线` |
| 22 | 让 lapse 标签在恢复后淡出、再次遗忘后重现 | `1cab60b` `fix: 修复 lapse 薄弱标签降级` |
| 23 | 让慢回忆标签在连续快速回忆后淡出、再次变慢后重现 | `10d4959` `fix: 修复慢回忆薄弱标签降级` |
| 24 | 让三类测验标签按模式连续答对降级、再次答错复发 | `00b7f2e` `fix: 修复测验薄弱标签永久保留` |
| 25 | 统一查词、lapse、慢回忆和三类测验的多维“已稳定”反馈 | `93ec0cb` `feat: 统一多维薄弱恢复反馈` |
| 26 | 回溯最近 4 个已完成冲刺处置周截至当前的复发率 | `4cc0852` `feat: 新增冲刺处置周复发率回溯` |

## 最终验证

- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npm test`：构建成功，178/178 通过，0 失败、0 跳过。
- 固定端口 3000：启动前无监听，正式服务健康检查 200；`tests/e2e/signal-flow.spec.mjs` 既有 8/8 通过（18.1s）。首次启动因执行沙箱回收未保持且未进入 E2E，按规则仅重启一次完成验证；结束后精确关闭两次记录的服务树并确认端口释放，PID/日志无残留。

## 最终咬合与边界

- `occlusion-table.md` 最终 14 项全部为 ✅，覆盖处置、恢复、复发、再处置、时间线、成效、入口一致性与全态反馈。
- 唯一受约束缺口：“猜错”只有 `guessMistakes` 累计次数，没有可靠事件时间源；当前不新增 schema，因此不接入时间线、降级或稳定反馈。
- 收尾不新增 schema，不改评分、FSRS 排程、备份、package scripts 或既有 E2E 语义；不新增功能、不改变业务语义。

## 交付状态与建议

- 当前分支：`codex/follow-up-hardening`；收尾提交应显式包含本报告、`overall-evaluation-26.md`、`occlusion-table.md` 与 `project-evolution.md`。
- 提交后工作区应仅保留用户的 untracked `1.txt`；该文件未读取、未修改、未纳入提交。
- 建议保留本地分支、不 push。未来若要处理“猜错”，应先单独评审真实事件时间源与 schema 迁移，而不是在本循环内补推断逻辑。
