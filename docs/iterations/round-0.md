# 第 0 轮报告：现场基线复查 + 咬合验证

> 本文件由主代理在首次运行「自主迭代循环」时生成；只读结论，禁止后续轮次改写历史。
> 生成日期：2026-08-08 ｜ 分支：codex/follow-up-hardening ｜ 执行方式：只读子代理（不修改文件、不 git 提交）

## ① git 现场

- 当前分支：`codex/follow-up-hardening`
- 工作区状态（未提交改动）：
  - 已修改：无（tracked 文件干净）
  - untracked：`1.txt`（用户文件，461B，禁止触碰）、`.dev-3000.pid`、两份历史 `.dev-3000` 日志、`docs/iterations/round-0.md`、`docs/iterations/occlusion-table.md`
- 未提交改动摘要（重点 `lib/weak-signals.ts`、`tests/weak-signals.test.ts` 的 diff）：
  - 无意外未提交改动；`lib/weak-signals.ts`、`tests/weak-signals.test.ts` 均无未提交 diff。
  - 当前分支较远端 ahead 22；HEAD 为 `b45fd84`。

## ② 测试基线

- `npm test`：未直接执行；其前置 build 会无条件重写 `lib/build-info.generated.ts`，与第 0 轮只读边界冲突。以相同测试主体直接运行，155/155 通过、0 失败/跳过（与文档记录一致：是）
- `npm run typecheck`：通过
- `npm run lint`：通过
- `npm run test:e2e`：按第 0 轮规则未运行

## ③ 历史核对（git log ↔ project-evolution.md）

| 轮次 | commit | 文档描述 | 是否一致 |
|---|---|---|---|
| 1~16 | `645fefe` 起的连续 16 个功能提交 | `project-evolution.md` 第 20~35 次迭代；每轮一个提交，message 与标题/内容相符 | ✅ |
| 17 | `6112edb` | 薄弱降级贯通；含代码、测试和迭代记录 | ✅ |
| 18 | `b45fd84` | 已稳定反馈；含代码、测试和迭代记录 | ✅ |

## ④ 漂移清单（文档与代码不一致，只列不修）

- [ ] 第 3~10 信号轮的文档日期写为 2026-08-09~16，实际提交集中在 2026-08-07~08。
- [ ] HEAD 已是 `b45fd84`，但 `lib/build-info.generated.ts` 仍绑定 `6112edb`。
- [ ] 文档称各入口复用同一薄弱派生；实际词书、划词候选、冲刺候选仍存在独立口径。

## ⑤ 咬合验证表（详情见 occlusion-table.md，此处只给汇总）

- ✅ 已咬合：5 条
- ❌ 断链：3 条（普通正向复习未进时间线；全态标签跨入口口径不一致；仅查 1 次也会出现“曾因查词被标记薄弱/已稳定”提示）
- ⚠️ 存疑：0 条

## ⑥ 缺口清单（用户可感知的不一致/缺失，作为后续轮次目标池）

- [ ] 划词候选固定查询阈值 2，未跟随设置页阈值，也不剔除已降级词。
- [ ] 时间线缺少普通正向复习与“猜错”事件。
- [ ] 非查词类薄弱消除后没有正向“已稳定”反馈。
- [ ] 冲刺入口计数可能包含没有任何展示标签的已降级词。

## 主代理结论

- 现场描述修正：第 17/18 轮已提交且 tracked 工作区干净；基线测试主体为 155/155；历史 1~18 轮连续且文档描述基本匹配。现有咬合状态为 5 项通过、3 项明确断链，不应直接按预览新增周级复发率回溯。
- 建议：用户确认后进入第 19 轮，优先修复咬合表中的判定/口径断链，再评估新增功能。
- 用户确认：是（2026-08-08，开始第 19 轮）
