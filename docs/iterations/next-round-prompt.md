# 第 59 轮待授权 Prompt：首次使用的三步轻量引导

## 等待授权

- 第 58 轮“词库读取失败后可在界面内重试”批次已达到授权上限 1/1；不得自动继续。
- 用户未明确授权第 59 轮或等价目标时，保持 `WAIT_AUTH`：不修改文件、不暂存、不提交、不启动服务。
- 启动时重新读取实际分支、完整 HEAD、tracked/index、`lib/build-info.generated.ts`、保护项和固定端口 3000，不把历史测试数字当作当前证据。

## 唯一候选目标

为首次使用用户提供可跳过、不过度打断的三步轻量引导，只解释：

1. 如何开始今日任务；
2. 如何先回忆再揭示释义并评分；
3. 如何使用查词与词本。

引导必须复用现有入口和状态，不伪造学习数据，不改变默认队列、FSRS、评分、推荐或持久化格式。先审计现有欢迎页、首次状态、键盘和移动端布局；如果是否持久化“已看过引导”需要新增 schema/version/store/domain，则记录业务/架构决策并 STOP，不自行扩展。

## 验证与边界

- 按 SOP v2 开启新批次并执行完整 Round 0；一个纵向目标、最多 1 轮。
- 使用风险分级验证，UI 最低达到 V3；精选 E2E 覆盖跳过、三步前进/后退、键盘和 320px。
- 精确暂存、一个中文提交、不 push；完成后评估并停止自动继续。
- 不在本轮实施 README Windows 双击启动入口或缺失 Node/构建失败说明。

## 后续候选（仅记录）

1. README 补充 Windows 双击启动入口和缺失 Node/构建失败的用户化说明。
