# 第 53 轮待授权 Prompt：阶段 F 条件收尾后的中风险取舍

## 当前真实现场

- 当前分支应为 `codex/follow-up-hardening`。
- 第 52 轮起始 HEAD 为 `758889102a9e41089210465ce2b314a866a587e4`；完成后应为唯一中文文档提交 `docs: 完成最小发布验证收尾`。启动时登记实际完整 hash，不猜测自引用提交。
- 当前最小发布证据链已闭环：typecheck 通过；lint 0 error / 1 个既有 warning；production build 与 Node 235/235 通过；production smoke 通过；7 项精选 E2E 7/7 通过。
- 固定 3000 的独立后台启动、完整 PID 登记与精确清理已验证；3000/3001 最终无监听，`lib/build-info.generated.ts` 已恢复。
- 阶段 F 可以“已验证最小护栏 + 明确中风险”条件收尾，但不得宣称全部发布就绪；未 push。

## 授权门禁

这是条件收尾后的候选 Prompt，不是自动执行指令。

- 用户未明确授权第 53 轮或等价目标时：保持等待，不修改文件、不暂存、不提交、不启动服务。
- 用户若接受条件收尾：无需继续迭代，只交付当前结论。
- 用户若要求继续：先从剩余中风险中选择一个独立目标；不得把多个缺口合并实现。

## 剩余中风险候选池

1. `activeSession` 孤儿 ID 与删除词后的索引/完成语义；
2. 非法持久化记录的行级隔离与可导出证据；
3. 重复 `quizAttempt` ID 在全部消费者中的统一语义；
4. `activeQuiz.startedAt` 非法时间契约；
5. “清空学习记录”是否保留 quizAttempts/activeQuiz 的产品语义；
6. 自动备份 5 份、恢复副本 10 份的容量与恢复窗口；
7. 真实长 reviews/quizAttempts 历史的设备性能与 SLA。

## 默认建议

接受阶段 F 条件收尾，不为轮数继续。只有用户明确选择某一风险，或发布决策要求解除该风险时，才创建下一轮单目标授权。

## 继续时的边界

- 先做该单一风险的只读契约审计；审计与实现分轮。
- 不读取用户生产数据、浏览器 IndexedDB 或真实备份，除非用户另行提供并明确授权只读使用。
- 不重复已通过的最小发布链；只有源码、测试或脚本变化后才按风险选择必要验证。
- 不降低产品门槛、不猜测历史、不自动迁移或回填、不 push。
