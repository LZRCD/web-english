# 下一轮执行 Prompt：第 37 轮近 7 天评分达标率口径审计

## 当前现场

- 分支：`codex/follow-up-hardening`
- 第 36 轮提交前 HEAD：`c41418d`；第 36 轮处于待提交状态，最终 HEAD 以包含本文件的最新 Git 提交为准。
- 最新验证：定向 weak-signals 77/77；lint、typecheck 通过；`npm test` 194/194；signal-flow E2E 15/15。
- 工作区交接目标：第 36 轮提交后仅保留用户未跟踪 `1.txt`；固定端口 3000 已释放。

## 第 36 轮已完成

- 新增共享 `buildPairedRecallChange`：目标侧按词内全部合法测时取均值，基线取严格边界前最近一条非冲刺合法测时，两侧齐全才配对，跨词等权。
- 周报/4 周使用对应周窗冲刺事件与周首冲刺边界；完成页使用完整 session 与 `session.createdAt` 边界；历史 sprint 不进入基线。
- 三入口统一为观察性同词配对文案并报告样本数；无配对返回 `null` 并显示“无配对样本”，不再保留用户可见旧非配对提升语义。

## 遗留断链与阶段判断

1. `buildLearningInsights.successRate` 实际统计当前自然日窗口内全部合法时间评分事件中 `rating≥2` 的事件占比，不按词去重，也不区分 new/review/sprint；UI 只写“成功率”。
2. 当前窗口无评分时内部返回 0%，页面无法区分“无样本”和“真实 0%”；上一窗口无样本时 delta 已返回 `null`。
3. 当前窗与上一窗比较的是各自评分事件总体占比；事件构成可不同，不能写成学习效果提升或后续保持变化。
4. 猜错仍只有累计次数，无真实事件时间、恢复或复发数据源。
5. `signal-flow` 的通用冲刺 seed 仍使用 `daysAgo(1)`；若在本地周一运行，seed 可能落入上周而使“本周冲刺观察”断言脆弱。这是既有测试夹具风险，不得静默改统计周窗；若第 37 轮 E2E 实际触发，应在不改变业务语义的前提下单独修正 seed 并保留 15 条链路。

阶段 A 的冲刺指标矩阵已收敛，但上述全局用户入口仍超过或模糊证据，因此暂不直接进入阶段 B。第 37 轮结束时必须重新判断阶段 A 是否完成。

## 下一轮唯一目标

只审计并诚实化轨迹页近 7 天全局评分事件达标占比：明确数据源、分子、分母、窗口、事件权重、空样本与上一窗口比较语义；让 UI 不再把无样本显示为 0%，不再使用含义过宽的“成功率”。不进入冲刺后首次正常复习保持，不改任何冲刺指标。

## 只读核对项

- 先检查 Git 状态、分支、HEAD、最近历史、端口 3000 与历史 PID；确认只有 `1.txt` 未跟踪。
- 从 `buildLearningInsights` 追踪 `validTimedReviews`、窗口边界、`successRate`、`successRateDelta` 到 `HistoryView` 的所有消费者和 `insights.test.ts` 覆盖。
- 核对 `rating≥2` 的分子、全部评分事件分母、事件重复权重、new/review/sprint 混合、未来时间排除、当前/上一窗口边界及本地自然日语义。
- 核对空当前窗、空上一窗、真实 0%、真实 100% 能否在现有类型/UI 中明确区分；检查是否还有其他“成功率”入口消费同一字段。

## 实现边界

- 只修近 7 天全局入口；推荐诚实语义为“评分达标事件占比”或经审计确定的同等精确文案，并披露 `rating≥2 / 全部评分事件`。
- 当前窗无评分必须返回/显示无样本，不能填 0；真实有样本且 0% 必须继续显示 0%。delta 只有当前窗和上一窗都有事件才存在，并明确是百分点差，不写“提升”。
- 不按词去重、不排除 sprint/new/review，除非只读审计证明现有产品语义和所有消费者要求另一套完整规则；禁止为了更好看擅改分母。
- 不建立后续随访、不进行同词保持配对、不进入维度归因或自适应推荐。
- 不新增 schema/version/store/domain；不改评分、FSRS、每日 Quiz 门禁、备份、package scripts、历史 reviews/quizAttempts、冲刺统计、薄弱画像或再冲刺。

## 验收标准

- 纯函数单测覆盖当前/上一自然日窗口、有样本 0%/100%、当前无样本、上一窗无样本、无效时间、边界和混合 session/kind 事件权重。
- UI/E2E 区分无样本与真实 0%，展示精确评分事件语义及百分点对比边界；既有 signal-flow 15 条语义不减少。
- 更新学习成效审计矩阵，并在本轮结束重新判断阶段 A 是否完成；只有矩阵所有可见指标已诚实且无其他高价值口径缺口，下一 prompt 才可进入阶段 B。
- 必须执行并报告实际数字：定向 `tests/insights.test.ts`（必要时联跑相关测试）、lint、typecheck、`npm test`、signal-flow E2E。
- 固定端口 3000 健康检查成功；结束后只清理本轮确认归属的 PID 和日志。

## 文档与提交要求

- 更新 `learning-effectiveness-audit.md`，创建 `round-37.md`，更新 `project-evolution.md` 和 `next-round-prompt.md`；仅在咬合状态变化时更新 `occlusion-table.md`。
- 完成验证与清理后先请求同一个 DeepSeek 任务做提交前复核；处理意见后显式暂存本轮文件。
- 一个中文 commit，不 push；禁止 `git add .`、`git add -A`、`git commit -am`，不修改或暂存 `1.txt`。

## 停止门槛

若完整链路需要新增 schema/version、改变评分/FSRS/每日门禁/备份、伪造历史样本、进入后续保持/维度归因/自适应推荐，或固定端口被非项目进程占用、浏览器连续两次失败，立即停止并只报告证据。若第 37 轮审计证明当前文案和空样本已经完全诚实且无需修改，也只提交审计与阶段结论，不制造功能改动。
