# 第 61 轮执行 Prompt：activeSession 失效词条安全恢复

## 当前授权

- 本轮属于用户已授权的连续批次第 3/3 轮；第 60 轮提交后通过 Delta Gate 即可继续，不重新执行完整 Round 0。
- 审计并修复进行中 `activeSession` 在刷新后遇到部分或全部 `wordId` 无法从当前词库解析的行为。

## 完成定义

- 部分失效：保留剩余有效词条的原顺序，并把进度夹取到有效范围。
- 全部失效：安全清除或结束会话，向用户说明会话词条已不可用、可以重新开始。
- 增加纯函数和精选浏览器测试，覆盖部分失效、全部失效、刷新和恢复提示。

## 边界与验证

- 不处理 `activeQuiz`；不新增 schema/version/store/domain；不改变正常队列顺序、FSRS、评分、推荐或学习数据。
- 先做最窄纯函数测试，再按 V3 执行 typecheck、lint、`test:unit` 和固定 3000 精选 E2E；不升级 V4。
- 精确暂存、一个中文提交、不 push；达到批次 3 轮上限后 STOP，后续候选只记录不实施。
