# 第 63 轮待决策 Prompt：activeQuiz 失效题组契约

## 当前状态

- 第 62 轮只读审计已完成，单轮批次达到 1/1；保持 `STOP / WAIT_DECISION`。
- 未获得产品决策与新的实施授权前，不修改代码、测试、数据结构或学习数据，不启动服务，不暂存新的实施内容。
- 审计报告：`docs/iterations/round-62.md`。

## 必须先决定

1. 部分目标失效后，index 是否按原 index 之前仍有效的题数重算；answers / correctCount 是裁剪重算，还是保留原题组历史分数与原分母。
2. 已完成题组部分失效时，结果页应保持原结果、按有效题重算，还是终止旧结果并要求重新开始。
3. 全部目标失效时，是否清除 `activeQuiz` 并提示“题组已不可用，可以重新开始”；若要保留，需说明现有产品中的实际恢复入口。
4. 答案快照的承诺范围：只保存用户已提交答案，还是连题干、正确答案与 `meaning-choice` 选项也必须在词条删除后完全保持。后者可能涉及持久化结构变化，必须单独授权。
5. 部分/全部失效的提示文案与可执行入口；不得直接复制 `activeSession` 语义。

## 等待规则

- 用户只作产品决策时，仅记录决策，不自动实施。
- 用户另行授权实施后，才重新执行完整 Round 0，并把一个获批契约拆成单一纵向目标；不得顺带处理其他 activeQuiz、activeSession、schema/version/store/domain、FSRS、评分、推荐或数据问题。
