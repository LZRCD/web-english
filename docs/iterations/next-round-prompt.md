# 第 63 轮结论待批准 Prompt：activeQuiz 失效题组恢复契约

## 当前状态

- 第 63 轮只读决策评估已完成，单轮批次达到 1/1；保持 `WAIT_DECISION`。
- 决策报告：`docs/iterations/round-63.md`。
- 报告中的推荐和文案语义尚未获批，不是实施授权；未修改产品代码、测试或持久化结构。

## 推荐方案（待批准）

1. 部分失效：有效题保持原相对顺序与 seed 偏移；index 按旧边界前仍有效题数重算；answers、correctCount、complete 与结果分母统一基于有效题集合；已写入的 attempt / review 不回滚。
2. 刷新恢复：必须等待 hydrate 与词库 ready；修正后一次写回，并提示题目减少及结果按剩余题计算。
3. 全部失效：清除 `activeQuiz`，保留历史作答事实，进入模式选择并提供“重新开始测验”入口，不保留无恢复入口的陈旧会话。
4. 快照范围：除已提交答案与提交时正确性外，也保证剩余有效题的题干、正确答案和 `meaning-choice` 选项与顺序不变。该承诺预计影响持久化会话结构与兼容链，需单独授权后再审计，不在本轮设计迁移。

## 可选决策

- 批准推荐方案；后续仍需另行明确授权实施。
- 选择最小方案：只保存已提交答案，接受剩余题由当前词数据重建、内容可能漂移。
- 选择保守方案：任一题失效就清除整组并要求重新开始。
- 提出其他产品取舍；此时先更新决策，不自动实施。

## 等待规则

- 未收到明确产品选择与独立实施授权前，保持 `WAIT_DECISION`。
- 不自动修改代码、测试、activeSession、schema/version/store/domain、FSRS、评分、推荐或用户学习数据，不自动进入其他候选。
