=== 2026-07-28 本轮实施结果 ===

已完成项：
✅ 解除 clock → persistedState 每分钟全量重写链路（stubbornWords 不再依赖 clock，persistedState 仅持久化 stubbornHistory）
✅ 存储层增量写入（putStore 替代 putAll，不再 clear 全量重写，错误重新抛出）
✅ 跨标签页冲突保护（revision CAS 检查 + BroadcastChannel 数据同步）
✅ FSRS 卡片逐词校验降级重建（个别卡损坏仅重建该词，不再全量清空 wordProgress）
✅ 导入/恢复立即落盘（importBackup/restoreBackup 先 await saveStoredStateImmediate 再提示成功）
✅ 备份校验增强（schema 版本拒绝、review ID 去重、FSRS 语义范围校验、canonicalWordId NaN 防御、parseStoredState null 守卫）
✅ 保存状态指示器（已保存时间 / 保存中 / 保存失败提示）
✅ useKeyboardShortcuts hook（忽略 button/select/input/textarea/[contenteditable]，弹层打开时暂停快捷键）
✅ AI 面板 inert（关闭时阻止 Tab 进入隐藏面板）
✅ 搜索弹窗焦点陷阱（Tab 循环 + body 滚动锁定 + 关闭后焦点恢复）

============================================================
未完成项及原因：

一、大规模重构（风险高，需逐状态迁移，本轮时间不足）
1. 拆分 2781 行单体页面
   原因：page.tsx 有 54 个 useState + 18 个 useEffect + 30+ 个 useMemo + 20+ 个事件处理函数，彼此耦合紧密。
   虽已具备 app/components/ 下的 8 个组件，但它们并未被 page.tsx 引入使用（page.tsx 全部内联 JSX）。
   真正的拆分需要：
   - 先抽 usePersistence hook（封装 hydration + 持久化链路，约 150 行）
   - 再抽 useStudyController hook（useReducer 管理 reviews/wordProgress/activeSession/positions，约 400 行）
   - 然后逐个对接现有组件（page.tsx 从 2781 → ~200 行）
   这是一次性的大手术，需要完整回归测试 + 手动功能验证，本轮优先保数据可靠性。
2. usePersistence hook
   依赖上述拆分节奏，单独抽取价值有限。
3. useStudyController hook（useReducer）
   同上，需要与组件拆分协同进行。
4. hash 路由（#study/#books 等支持深链和浏览器前进后退）
   属于增量功能，优先度低于数据可靠性。

二、构建期优化（需修改数据管线 + 验证音频/词库不损坏）
5. 缩短首次可学习时间（构建期合并审计修正、音频后台加载、按分组分片）
   原因：涉及 scripts/extract-redbook.mjs + build-audio-index.mjs 的改动，需要重跑完整数据管线
   并验证 6550 条词目 + 6326 条音频无损坏。非代码层的风险，需单独安排时间。
6. ECDICT 双字母分片
   原因：需改 build-ecdict-shards.mjs + page.tsx 查词逻辑，当前单字母分片可用只是偏慢（约 5.48 MiB 分片）。

三、UI/视觉优化（不改功能，但需要逐条 CSS 回归验证）
7. 设计令牌补齐（166 个独立十六进制颜色 → CSS 自定义属性）
   原因：纯视觉重组，不改外观。但需逐色值审计 + 确认全量回归，避免误改。
8. 移动端优化（1100px 折叠学习工具、词本批量操作随标签变化、底栏精简）
9. 活动日历 365 按钮 roving tabindex + 方向键导航 + 24px 触控区域
10. 按钮语义 HTML（h1/h2/div 改 span）+ 词本 tabs aria-controls/roving tabindex
11. 小字可读性（10px→12px，对比度 WCAG AA）
12. 字体固定（Noto Sans SC 通过 next/font 本地加载）
13. 移动端底栏精简（5+AI → 4 个主入口）

四、小功能（本轮排不上，但值得做）
14. 词书页增加"单元进度、待复习数、继续上次位置"
15. 保存状态提示（已完成）
16. 每日统计独立聚合保存（不依赖 10,000 条 reviews 列表 —— 属于数据层改动，但 1.3 已大幅降低风险）
17. 划词集 200 条淘汰级联清理
18. 红宝书划词 ID 携带原始 wordId + March/may 同形词处理

五、未做但应优先考虑的后续项
19. 10,000 条评分上限解决（reviewArchive 聚合历史计数）
   原因：需要改 StoredState schema + IndexedDB store 布局，影响 storage.ts/study.ts/learning.ts 三文件。
   本轮 1.4（FSRS 逐词校验）已大幅降低此问题的风险——现在个别卡片损坏不会连带清空所有进度。
20. 多标签页冲突时的自动合并（而不是全量替换）
   原因：1.3 已加了 CAS 检测 + 通知，但合并逻辑比较复杂（要判断哪些评分是新增的）。
   当前方案是"冲突时通知用户刷新"，不会静默覆盖，可接受。
21. manifest/service worker（PWA 离线缓存）
   原因：全量字典 54.73 MiB + 音频 133.39 MiB，一次性预缓存不现实。需设计按需缓存策略。
