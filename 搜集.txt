# 评估网站并改进功能

> 对这个网站进行评估，对结构上，数据上能改进的改进，可以加一点功能，不要打开网页预览，可能会卡住

<details><summary>4 previous messages</summary>

> 我会先做代码层面的结构、数据流和交互审计，再直接实施高价值改进；按你的要求不打开网页预览，只用静态检查、测试与构建验证。这个任务适合用 `frontend-design` 技能，我会用它约束界面与交互改造，并把整个过程登记为持续目标。
>
> 项目已连接 Sites（存在 `.openai/hosting.json`）。因此我也会按 `sites-building` 与 `sites-hosting` 技能处理现有项目状态和最终发布；这不会启动本地预览，也不会用浏览器打开页面。先完成代码审计和构建验证，发布只基于已验证的源码版本。
>
> 第一轮盘点已经确认：核心功能很完整，但当前页面是一个超大的单文件组件，状态与界面耦合很重；数据层虽已分域存储，仍需重点检查写入一致性、迁移和导入边界。接下来我会先跑基线检查，再依据并行审计结果收敛改造范围。
>
> 审计已经收敛。最关键的问题不是视觉，而是“静默数据风险”：导入未真正落盘就提示成功、存储异常被吞掉、同形词大小写会串义、红宝书划词可能生成第二份学习进度。结构上我会保留现有“记忆轨道”视觉，把本轮改造集中在数据可靠性、首屏加载和一个可行动的学习趋势面板。
</details>
